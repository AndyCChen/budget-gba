[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19", default-features = false, features = ["2d", "ui"] }
gba = { path = "../gba" }

[features]
dev = ["bevy/dynamic_linking"]
